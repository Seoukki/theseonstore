# ═══════════════════════════════════════════════════════
#  SeonsPrems — Environment Variables (.env.example)
#  Copy this file to .env and fill in your values.
# ═══════════════════════════════════════════════════════

# ── Supabase ────────────────────────────────────────────
# Your Supabase project URL — found in Project Settings > API
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Supabase anon/public key — safe for read queries
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service role key — KEEP SECRET, server-side only
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Admin ────────────────────────────────────────────────
# Strong password for admin panel access (min 12 chars)
ADMIN_PASSWORD=your_strong_admin_password_here

# ── Payment Gateway (NeoXR/Tako) ────────────────────────
# Your merchant name on NeoXR dashboard
TAKO_MERCHANT=your_merchant_name

# Your NeoXR API key
TAKO_API_KEY=your_tako_api_key

# ── Store Info ───────────────────────────────────────────
STORE_NAME=SeonsPrems

# WhatsApp number (include country code, no +)
WA_NUMBER=628123456789

# Telegram username (no @)
TELEGRAM_USER=yourtelegram

# Instagram username (no @)
IG_USER=yourinstagram

# ═══════════════════════════════════════════════════════
#  SUPABASE TABLE SETUP (run in SQL Editor)
# ═══════════════════════════════════════════════════════
#
# -- Products
# CREATE TABLE products (
#   id          uuid DEFAULT gen_random_uuid() PRIMARY KEY,
#   name        text NOT NULL,
#   category    text NOT NULL DEFAULT 'other',
#   description text,
#   image_url   text,
#   prices      jsonb DEFAULT '{}',
#   badge       text DEFAULT '',
#   active      boolean DEFAULT true,
#   rules       text DEFAULT '',
#   created_at  timestamptz DEFAULT now(),
#   updated_at  timestamptz DEFAULT now()
# );
#
# -- Broadcasts
# CREATE TABLE broadcasts (
#   id         uuid DEFAULT gen_random_uuid() PRIMARY KEY,
#   message    text NOT NULL,
#   active     boolean DEFAULT true,
#   created_at timestamptz DEFAULT now()
# );
#
# -- Orders
# CREATE TABLE orders (
#   id             uuid DEFAULT gen_random_uuid() PRIMARY KEY,
#   txn_id         text UNIQUE NOT NULL,
#   product_id     uuid REFERENCES products(id),
#   product_name   text,
#   category       text,
#   email          text NOT NULL,
#   whatsapp       text,
#   duration       text,
#   duration_label text,
#   quantity       int  DEFAULT 1,
#   total          bigint,
#   status         text DEFAULT 'pending',
#   created_at     timestamptz DEFAULT now(),
#   paid_at        timestamptz
# );
#
# -- Account Pool (auto-delivered stock)
# CREATE TABLE account_pool (
#   id           uuid DEFAULT gen_random_uuid() PRIMARY KEY,
#   product_id   uuid REFERENCES products(id),
#   account_data jsonb NOT NULL,
#   used         boolean DEFAULT false,
#   order_id     uuid REFERENCES orders(id),
#   used_at      timestamptz,
#   created_at   timestamptz DEFAULT now()
# );
#
# -- Indexes
# CREATE INDEX idx_orders_txn      ON orders(txn_id);
# CREATE INDEX idx_orders_email    ON orders(email);
# CREATE INDEX idx_orders_status   ON orders(status);
# CREATE INDEX idx_pool_product    ON account_pool(product_id, used);
# CREATE INDEX idx_broadcasts_act  ON broadcasts(active, created_at DESC);
#
# -- RLS (optional, recommended)
# ALTER TABLE products      ENABLE ROW LEVEL SECURITY;
# ALTER TABLE orders        ENABLE ROW LEVEL SECURITY;
# ALTER TABLE account_pool  ENABLE ROW LEVEL SECURITY;
# ALTER TABLE broadcasts    ENABLE ROW LEVEL SECURITY;
